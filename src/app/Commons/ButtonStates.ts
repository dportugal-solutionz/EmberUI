/**
 * Button states.
 *
 */
export enum ButtonStates {
    Idle = "idle",      //not pressed, not selected
    Pressed = "pressed",   //is pressed, could be selected
    Selected = "selected"  //not pressed, is selected
}